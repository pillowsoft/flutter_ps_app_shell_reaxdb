# Flutter App Shell - Environment Configuration Template
# Copy this file to .env and configure your values

# ==============================================================================
# ReaxDB Configuration (Local Database)
# ==============================================================================

# Database name (default: app_shell)
# This determines the database file name in the application documents directory
REAXDB_DATABASE_NAME=app_shell

# Enable encryption (default: false)
# When enabled, all database data is encrypted at rest using AES-256
REAXDB_ENCRYPTION_ENABLED=false

# Encryption key (required if encryption enabled)
# Must be a secure random string. Keep this secret and never commit to version control.
# Generate a secure key: openssl rand -base64 32
# REAXDB_ENCRYPTION_KEY=your-secure-32-character-key-here

# ==============================================================================
# ReaxDB WAL (Write-Ahead Log) Management
# ==============================================================================

# Auto-cleanup stale WAL files before database initialization (default: true in debug mode)
# Prevents WAL accumulation during development (hot restarts, Ctrl+C exits)
REAXDB_WAL_AUTO_CLEANUP=true

# Maximum WAL file size threshold in MB (default: 10)
# Warns if total WAL size exceeds this value
REAXDB_WAL_MAX_SIZE_MB=10

# Checkpoint WAL on database close (default: true)
# Merges WAL changes back into main database on clean shutdown
REAXDB_CHECKPOINT_ON_CLOSE=true

# ==============================================================================
# Development & Debugging Options
# ==============================================================================

# Enable debug logging for App Shell services (default: false)
DEBUG_LOGGING=false

# Log level: error, warning, info, debug (default: info)
LOG_LEVEL=info

# ==============================================================================
# Cloudflare Workers Configuration (Optional)
# ==============================================================================

# Cloudflare Worker API URL (optional, for backend integration)
# CLOUDFLARE_WORKER_URL=https://your-worker.workers.dev/api/

# Session JWT configuration (required for Cloudflare integration)
# SESSION_JWT_SECRET=your-jwt-secret-here
# SESSION_JWT_ISSUER=your-app-name
# SESSION_JWT_AUDIENCE=your-app-name

# ==============================================================================
# Migration Notes
# ==============================================================================
# This project migrated from InstantDB to ReaxDB in v2.0.0
# InstantDB features removed:
# - INSTANTDB_APP_ID (cloud database with real-time sync)
# - INSTANTDB_ENABLE_SYNC (cloud synchronization)
# - INSTANTDB_VERBOSE_LOGGING
# - Magic link authentication (cloud-based)
#
# All data is now stored locally using ReaxDB for:
# ✅ High performance (21,000+ writes/sec, 333,000+ cached reads/sec)
# ✅ Zero native dependencies (pure Dart)
# ✅ Optional AES-256 encryption
# ✅ No internet connection required
# ✅ Complete privacy (data never leaves device)
#
# For migration details, see: REAXDB_MIGRATION_PLAN.md