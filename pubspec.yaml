name: flutter_ps_app_shell
description: Flutter PowerSchool App Shell Framework - A comprehensive application framework with zero-configuration foundation for Flutter apps
version: 2.0.1
publish_to: 'none'

environment:
  sdk: ^3.6.0
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter

  # The actual package implementation
  flutter_app_shell:
    path: packages/flutter_app_shell

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
