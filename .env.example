# Flutter App Shell - Environment Configuration Template
# Copy this file to .env and configure your values
#
# This file should be placed in the root of your Flutter project
# that uses the flutter_app_shell package

# ==============================================================================
# ReaxDB Configuration (Local-Only Database)
# ==============================================================================

# Database name (stored in application documents directory)
# Default: app_shell
REAXDB_DATABASE_NAME=app_shell

# Enable encryption for data at rest
# When enabled, all database content is encrypted using AES-256
# Default: false
REAXDB_ENCRYPTION_ENABLED=false

# Encryption key (required if encryption is enabled)
# IMPORTANT: Keep this secret and never commit to version control
# Generate a secure key: openssl rand -base64 32
# REAXDB_ENCRYPTION_KEY=

# ==============================================================================
# REMOVED: InstantDB Configuration (Replaced by ReaxDB)
# ==============================================================================
# The following InstantDB features have been REMOVED in v2.0.0:
# - INSTANTDB_APP_ID (cloud database)
# - INSTANTDB_ENABLE_SYNC (real-time sync)
# - INSTANTDB_VERBOSE_LOGGING (cloud logging)
# - Magic link authentication (cloud-based auth)
#
# All data is now stored locally using ReaxDB.
# Authentication is local-only (email/password, biometric).
# See REAXDB_MIGRATION_PLAN.md for migration details.

# ==============================================================================
# Development & Debugging Options
# ==============================================================================

# Force local-only mode even when INSTANTDB_APP_ID is provided
# Useful for offline development and testing
# Uncomment to enable:
# FORCE_LOCAL_ONLY=true

# Enable debug logging for App Shell services
# Shows detailed logs for authentication, database operations, etc.
# Default: false
DEBUG_LOGGING=false

# Log level for App Shell services
# Options: error, warning, info, debug
# Default: info
LOG_LEVEL=info

# ==============================================================================
# Optional Service Configuration
# ==============================================================================

# Analytics service configuration (if using analytics plugin)
# ANALYTICS_ENABLED=true
# ANALYTICS_API_KEY=

# Push notification service (if implementing push notifications)
# PUSH_NOTIFICATIONS_ENABLED=false
# FCM_SERVER_KEY=

# Custom API endpoints (if integrating with external services)
# API_BASE_URL=https://api.yourservice.com
# API_TIMEOUT=30000

# ==============================================================================
# Platform-Specific Configuration
# ==============================================================================

# Web-specific settings
# WEB_RENDERER=canvaskit
# WEB_BASE_HREF=/

# Desktop app settings  
# WINDOW_TITLE=Flutter App Shell
# WINDOW_WIDTH=1200
# WINDOW_HEIGHT=800
# WINDOW_MIN_WIDTH=800
# WINDOW_MIN_HEIGHT=600

# ==============================================================================
# Setup Instructions
# ==============================================================================

# 1. Copy this file to .env in your project root:
#    cp .env.example .env
#
# 2. Configure database settings:
#    - Set REAXDB_DATABASE_NAME (optional, defaults to 'app_shell')
#    - Enable REAXDB_ENCRYPTION_ENABLED if you need encrypted storage
#    - Generate and set REAXDB_ENCRYPTION_KEY if encryption is enabled
#
# 3. Restart your Flutter app to load the new configuration
#
# 4. Database location:
#    - Android: /data/data/<package>/app_flutter/[REAXDB_DATABASE_NAME]
#    - iOS: Library/Application Support/[REAXDB_DATABASE_NAME]
#    - macOS: ~/Library/Containers/<app>/Data/Library/Application Support/[REAXDB_DATABASE_NAME]
#    - Windows: %APPDATA%\<app>\[REAXDB_DATABASE_NAME]
#    - Linux: ~/.local/share/<app>/[REAXDB_DATABASE_NAME]

# Need help?
# - Documentation: See CLAUDE.md and REAXDB_MIGRATION_PLAN.md
# - ReaxDB Docs: https://pub.dev/packages/reaxdb_dart